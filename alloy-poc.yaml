alloy:
  configMap:
    create: false
    name: alloy-config
    key: config.alloy
image:
  # -- Grafana Alloy image registry (defaults to docker.io)
  registry: "docker.io"
  repository: grafana/alloy
  pullPolicy: IfNotPresent
  pullSecrets: []